apiVersion: v1
kind: ConfigMap
# ... (metadata and labels) ...
data:
{{- $files := .Files.Glob "jcasc/*.yaml" }}
{{- range $path, $_ := $files }}
  {{ base $path }}: |-
{{ ($.Files.Get $path | toString) | indent 4 }}
{{- end }}
